[project]
name = "media-recommender"
version = "0.0.0"
readme = "README.md"
authors = [{ name = "pokornyIt" }]
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.14.2,<3.15"

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pydoclint==0.9.1",
    "pre-commit>=4.6,<5",
    "pyright>=1.1.404,<2",
    "pytest>=9,<10",
    "pytest-cov>=7,<8",
    "ruff>=0.16,<0.17",
]
